package com.ilp.services;

import java.security.Provider.Service;
import java.util.ArrayList;
import java.util.Scanner;

import com.ilp.entity.Product;
import com.ilp.entity.Services;

public class ProductConfiguration {

	public static Product createProduct(ArrayList<Services> addedServiceList ) {
		// TODO Auto-generated method stub
		
		Product product =null;
		
		Scanner scanner = new Scanner(System.in);
		System.out.println("createproduct working");
		for (Services service : addedServiceList) {
			System.out.println(service.getServiceCode() + " \t "+service.getServiceName() + " \t "+service.getRate());
		}
//		int choice = scanner.nextInt();
//char goToMenu;
//		ArrayList<Services> serviceList = new ArrayList<Services>();
//		do {
////			serviceList.add(ProductConfiguration.createService());
//			System.out.print("Do you want to continue(y/n):");
//			goToMenu=scanner.next().charAt(0);
//		}while(goToMenu == 'y');
//		
//		
//		switch(choice) {
//		case 1:
//			 product=new Product("PR100","Savings Max Account",serviceList);
//			break;
//		case 2:
//			product=new Product("PR200","Current Account",serviceList);
//			break;
//		case 3: 
//			product=new Product("PR300","Loan Account",serviceList);
//			break;
//		}
		return product;

	}
	
	
	public static ArrayList<Services> createService() {
		// TODO Auto-generated method stub

		Scanner scanner = new Scanner(System.in);
	
		ArrayList<Services> addedServiceList = new ArrayList<Services>();
		Services services =null;
		char choice;
		
		do { 
			System.out.println("Enter the service code");
			String serviceCodeUser = scanner.next();
			
			System.out.println("Enter the service Name");
			String serviceNameUser = scanner.next();
			System.out.println("Enter the service Rate");
			double serviceRateUser = scanner.nextDouble();
			services  = new Services(serviceCodeUser,serviceNameUser,serviceRateUser);
			addedServiceList.add(services);
			System.out.println("do you want to continue");
			choice = scanner.next().charAt(0);
		} while (choice == 'y');
		
		return  addedServiceList;
	}

}
